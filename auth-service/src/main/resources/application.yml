
server:
  servlet:
    session:
      persistent: true
  port: 8098
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://172.20.10.3:5434/spice_af_dev_local?serverTimezone=UTC
    username : postgres
    password : root
logging:
  level:
    root: DEBUG

